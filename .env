# Adapta Hub (identidade). Sem isto, o painel segue em modo demo (mock).
VITE_HUB_API_URL=http://localhost:3334
# Override do spoke (o spokeUrl do tenant pode ser um tunnel do celular).
VITE_SPOKE_API_URL=http://localhost:3333
